/** Contains all info that is needed to spawn any ascii or other characters to the level. */
export interface EntityConfig {
    textStyle?: Phaser.Types.GameObjects.Text.TextStyle,
    description?: string
}